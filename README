My vim files.

Managed by Vundle, so install that on a new checkout:

    $ git submodule update --init
    $ vim +PluginInstall +qall

If your vim doesn't check `~/.vim/vimrc`, symlink it:
    $ ln -s vimrc ~/.vimrc
